# Contributing

## Local setup

- Recommended: use Docker (`docker-compose up`) or Python 3.11.
- Install dev tools: `python -m pip install -e ".[dev]"`
- Run formatting + lint: `ruff format . && ruff check .`
- Run tests: `pytest`

## Pull requests

- Keep PRs focused and small.
- Update docs when behavior changes.
